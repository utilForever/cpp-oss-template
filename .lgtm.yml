extraction:
  cpp:
    configure:
      command:
        - mkdir _lgtm_build_dir
        - cd _lgtm_build_dir
    index:
      build_command:
        - cd _lgtm_build_dir
        - make

path_classifiers:
  library:
    - Libraries
